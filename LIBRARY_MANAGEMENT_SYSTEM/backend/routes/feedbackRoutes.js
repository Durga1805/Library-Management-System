// const express = require('express');
// const { submitFeedback, getAllFeedback } = require('../controllers/feedbackController');
// const { isAuthenticated } = require('../middleware/authMiddleware'); // Assume authentication is handled

// const router = express.Router();

// // POST route to submit feedback
// router.post('/', isAuthenticated, submitFeedback);

// // GET route to fetch all feedback (admin/staff only)
// router.get('/', isAuthenticated, getAllFeedback);

// module.exports = router;
